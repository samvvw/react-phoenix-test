{
    "name": "react-phoenix-test",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "esbuild src/app.jsx --bundle --outfile=dist/build.js --sourcemap",
        "build:dev": "esbuild src/app.jsx --bundle --outfile=dist/build.js --sourcemap --watch"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "esbuild": "^0.15.6",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "@types/react": "^18.0.18"
    }
}
